[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prep"
version = "0.1.0"
description = "Prep Enterprise Compliance Platform – orchestration, engines, governance"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "ruff>=0.6.0",
  "mypy>=1.10.0",
  "bandit>=1.7.5"
]

[tool.pytest.ini_options]
addopts = "-q"
python_files = "test_*.py tests_*.py *_test.py"
