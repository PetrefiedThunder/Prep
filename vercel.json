{
  "version": 2,
  "builds": [
    {
      "src": "prepchef/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/prepchef/dist/$1"
    }
  ]
}
