{
  "title": "Optimizers SLI",
  "panels": [
    {
      "type": "stat",
      "title": "Pricing Conversion",
      "targets": [
        {
          "expr": "sum(rate(pricing_converted[5m]))/sum(rate(pricing_offered[5m]))"
        }
      ]
    },
    {
      "type": "graph",
      "title": "API p95",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_ms_bucket[5m])) by (le))"
        }
      ]
    }
  ]
}
