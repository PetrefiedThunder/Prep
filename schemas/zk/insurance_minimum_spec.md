# Insurance Minimum ZK Proof Specification

- Public inputs: `threshold`, `expiry_date`
- Private inputs: `policy_amount`, `policy_expiry`
- Statement: `policy_amount >= threshold AND policy_expiry >= today()`
